# Copy to frontend/.env for the default isolated verification stack, or inject
# a different loopback backend origin at process start.
VITE_API_BASE=http://127.0.0.1:8000
VITE_ENABLE_GOOGLE_AUTH=false
VITE_GOOGLE_CLIENT_ID=
